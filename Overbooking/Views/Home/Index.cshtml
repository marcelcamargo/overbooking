@{
    ViewBag.Title = "Voos";
}

<div class="container">
    <h1 class="page-header titulo">Lista de voos</h1>

    <div class="row">
        <div class="table-responsive">
            <table id="tabelaVoos" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Nome do produto</th>
                        <th>Preço</th>
                        <th>Marca</th>
                    </tr>
                </thead>
                <tbody>
                    @*@foreach (var produto in Model)
        {
            <tr>
                <td>@produto.Nome</td>
                <td>R$ @string.Format("{0:N}", produto.Valor)</td>
                <td>@produto.Marca.Nome</td>
            </tr>
        }*@

                    @{
                        for (int i = 0; i < 5; i++)
                        {
                            <tr>
                                <td>@($"item {i.ToString()}")</td>
                                <td>@($"seleção {i.ToString()}")</td>
                                <td>@($"% {i.ToString()}")</td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>